export interface ResponsiveControlsConfig {
  enablePan: boolean;
  enableZoom: boolean;
  enableRotate: boolean;
  minDistance: number;
  maxDistance: number;
  dampingFactor: number;
  rotateSpeed: number;
  zoomSpeed: number;
  touchAction: string;
}
